#include <iostream>
int  plus[200];
int main() {
	int num;
	std::cin >> num;
	for (int i = 0;i < num;i++) {
		int many,max[100];
		std::cin >> many;
		for (int j = 0;j < 2*many;j++)std::cin >> plus[i]; 
		
	}
}
